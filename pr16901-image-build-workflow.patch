--- a/.github/workflows/schedule_image_build_and_push.yaml
+++ b/.github/workflows/schedule_image_build_and_push.yaml
@@ -1,6 +1,6 @@
 # This is a docker build check and publish job:
-# 1. PR Triggered docker image build check
-#   - 'image-build' label should be added
+# 1. PR push triggered docker image build check (temporary for PR #16901)
+#   - Select targets with the PR_IMAGE_* / PR_BUILD_* variables below
 #   - is for image build check
 #   - Enable on main/releases branches
 #   - push: ${{ github.event_name != 'pull_request' }} ==> false
@@ -22,7 +22,7 @@
     branches:
       - 'main'
       - 'releases/*'
-    types: [ labeled ]
+    types: [synchronize]
   workflow_dispatch:
     inputs:
       tag:
@@ -100,6 +100,12 @@
         default: '["a2","a3","310p","a5"]'
         type: string
 
+# Temporary PR #16901 controls. Change these before each PR push; remove before merge.
+env:
+  PR_IMAGE_BUILD: 'true'
+  PR_BUILD_CHIPS: '["a3"]'
+  PR_BUILD_OSES: '["ubuntu"]'
+
 permissions:
   packages: write
   contents: read
@@ -107,8 +113,8 @@
 jobs:
   setup-matrix:
     runs-on: linux-amd64-cpu-2-hk
-    if: ${{ github.event_name != 'pull_request' || github.event.label.name == 'image-build' }}
     outputs:
+      build_enabled: ${{ steps.filter.outputs.build_enabled }}
       build_meta: ${{ steps.filter.outputs.build_meta }}
     steps:
       - name: Resolve effective build_type
@@ -126,24 +132,41 @@
       - name: Filter build matrix by chips
         id: filter
         run: |
-          CHIPS='${{ inputs.chips || '["a2","a3","310p","a5"]' }}'
+          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
+            if [[ "$PR_IMAGE_BUILD" != "true" ]]; then
+              echo 'build_enabled=false' >> "$GITHUB_OUTPUT"
+              echo 'build_meta=[]' >> "$GITHUB_OUTPUT"
+              exit 0
+            fi
+            CHIPS="$PR_BUILD_CHIPS"
+            OSES="$PR_BUILD_OSES"
+          else
+            CHIPS='${{ inputs.chips || '["a2","a3","310p","a5"]' }}'
+            OSES='["ubuntu","openeuler"]'
+          fi
           ALL_MATRIX=$(jq -nc '[
-            {name: "A2 Ubuntu",      dockerfile: "Dockerfile",              suffix: "",               chip: "a2", soc: "910b", base_os: "ubuntu22.04"},
-            {name: "A3 Ubuntu",      dockerfile: "Dockerfile.a3",            suffix: "a3",             chip: "a3", soc: "a3",   base_os: "ubuntu22.04"},
-            {name: "310P Ubuntu",    dockerfile: "Dockerfile.310p",          suffix: "310p",           chip: "310p", soc: "310p", base_os: "ubuntu22.04"},
-            {name: "A5 Ubuntu",      dockerfile: "Dockerfile.a5",            suffix: "a5",             chip: "a5", soc: "950",  base_os: "ubuntu22.04"},
-            {name: "A2 openeuler",   dockerfile: "Dockerfile.openEuler",     suffix: "openeuler",      chip: "a2", soc: "910b", base_os: "openeuler24.03"},
-            {name: "A3 openEuler",   dockerfile: "Dockerfile.a3.openEuler",  suffix: "a3-openeuler",   chip: "a3", soc: "a3",   base_os: "openeuler24.03"},
-            {name: "310P openEuler", dockerfile: "Dockerfile.310p.openEuler",suffix: "310p-openeuler",  chip: "310p", soc: "310p", base_os: "openeuler24.03"},
-            {name: "A5 openEuler",   dockerfile: "Dockerfile.a5.openEuler",  suffix: "a5-openeuler",   chip: "a5", soc: "950",  base_os: "openeuler24.03"}
+            {name: "A2 Ubuntu",      dockerfile: "Dockerfile",              suffix: "",               chip: "a2", soc: "910b", os: "ubuntu",    base_os: "ubuntu22.04"},
+            {name: "A3 Ubuntu",      dockerfile: "Dockerfile.a3",            suffix: "a3",             chip: "a3", soc: "a3",   os: "ubuntu",    base_os: "ubuntu22.04"},
+            {name: "310P Ubuntu",    dockerfile: "Dockerfile.310p",          suffix: "310p",           chip: "310p", soc: "310p", os: "ubuntu",    base_os: "ubuntu22.04"},
+            {name: "A5 Ubuntu",      dockerfile: "Dockerfile.a5",            suffix: "a5",             chip: "a5", soc: "950",  os: "ubuntu",    base_os: "ubuntu22.04"},
+            {name: "A2 openeuler",   dockerfile: "Dockerfile.openEuler",     suffix: "openeuler",      chip: "a2", soc: "910b", os: "openeuler", base_os: "openeuler24.03"},
+            {name: "A3 openEuler",   dockerfile: "Dockerfile.a3.openEuler",  suffix: "a3-openeuler",   chip: "a3", soc: "a3",   os: "openeuler", base_os: "openeuler24.03"},
+            {name: "310P openEuler", dockerfile: "Dockerfile.310p.openEuler",suffix: "310p-openeuler",  chip: "310p", soc: "310p", os: "openeuler", base_os: "openeuler24.03"},
+            {name: "A5 openEuler",   dockerfile: "Dockerfile.a5.openEuler",  suffix: "a5-openeuler",   chip: "a5", soc: "950",  os: "openeuler", base_os: "openeuler24.03"}
           ]')
-          FILTERED=$(echo "$ALL_MATRIX" | jq -c --argjson chips "$CHIPS" '[.[] | select(.chip as $c | $chips | index($c))]')
-          echo "build_meta=$FILTERED" >> $GITHUB_OUTPUT
+          FILTERED=$(echo "$ALL_MATRIX" | jq -c --argjson chips "$CHIPS" --argjson oses "$OSES" \
+            '[.[] | select((.chip as $c | $chips | index($c)) and (.os as $o | $oses | index($o)))]')
+          if [[ "$(echo "$FILTERED" | jq 'length')" -eq 0 ]]; then
+            echo '::error::No image build targets match PR_BUILD_CHIPS and PR_BUILD_OSES'
+            exit 1
+          fi
+          echo 'build_enabled=true' >> "$GITHUB_OUTPUT"
+          echo "build_meta=$FILTERED" >> "$GITHUB_OUTPUT"
 
   image_build:
     name: ${{ matrix.build_meta.name }}
     needs: setup-matrix
-    if: ${{ !((github.event_name == 'pull_request' && !contains(github.event.pull_request.labels.*.name, 'image-build')))
+    if: ${{ needs.setup-matrix.outputs.build_enabled == 'true'
         && !(github.event_name == 'workflow_dispatch' && inputs.vllm_commit != '' && inputs.vllm_ascend_commit != '') }}
     strategy:
       fail-fast: false
